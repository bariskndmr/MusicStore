import { StyleSheet } from "react-native";

export default StyleSheet.create({
    input : {
        backgroundColor: '#eceff0',
        padding : 10,
        marginHorizontal: 5,
        borderRadius : 5,
    }
});